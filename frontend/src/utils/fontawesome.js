import { library } from "@fortawesome/fontawesome-svg-core";
import {
  faUser,
  faLock,
  faEnvelope,
  faHouse,
  faKey,
} from "@fortawesome/free-solid-svg-icons";

// Thêm các icon sẽ dùng vào library
library.add(faUser, faLock, faEnvelope, faHouse, faKey);
